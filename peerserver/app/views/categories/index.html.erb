<table width=100%><tr><td width=75%><h1>Peer Exchange</h1></td><td valign="top" align="right" width =75% ><% if logged_in? %>
Welcome <%= current_application.username %>. <%= link_to "Log out", logout_path %>
<% else %>
<%= link_to "Sign up", signup_path %> or
<%= link_to "Log in", login_path %>
<% end %></td></tr></table>
<%= link_to "Home", "/" %>
<%= link_to "Category List", categories_path %>
<% if logged_in? %>
<%else%>
<%= link_to "User Registration", signup_path %>
<%end%>

<p> <br><%= link_to 'Create Category', new_category_path %></p>
<table>
  <tr>
    <th></th>
    <th></th>
  </tr>

<% @categories.each do |category| %>
  <tr>
    <td><%= category.name %></td>
    <td><img src='<%= category.url %>' width=50 height=50> </td>
    <td><br></td>
  </tr>
<% end %>
</table>

<br />

