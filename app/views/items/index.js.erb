//alert("from items index.js.erb");

//<% unless (defined?(@current_highest_bid)).nil? %>
//$("#current-price-value").html("<%= escape_javascript(number_to_currency(@current_highest_bid, :unit=>"$"))%>");
//<% end %>